import { useState } from 'react'
import { RiAddLine, RiShoppingBagLine } from '@remixicon/react'
import { RoleLayout } from './RoleLayout'

export function SellerWorkspace() {
  const [stock, setStock] = useState(12)
  return <RoleLayout title="Your seller workspace" description="Manage your listings, keep stock accurate, and respond to the repair community quickly."><div className="grid gap-4 md:grid-cols-3">{[['18', 'Live listings', '3 need a stock check'], ['5', 'New orders', '2 received today'], ['96%', 'Response rate', 'Above marketplace average']].map(([value, label, detail]) => <article key={label} className="rounded-xl border border-[#e0e6e1] bg-white p-5"><p className="text-2xl font-semibold tracking-[-.03em] text-[#1e2c23]">{value}</p><p className="mt-1 text-sm font-semibold text-[#38473d]">{label}</p><p className="mt-3 text-xs text-[#768278]">{detail}</p></article>)}</div><article className="mt-7 overflow-hidden rounded-xl border border-[#dce5de] bg-white"><div className="flex items-center justify-between border-b border-[#eaf0eb] bg-[#f5faf6] px-5 py-4"><div><p className="text-xs font-bold uppercase tracking-[.12em] text-[#5c806a]">Inventory snapshot</p><h2 className="mt-1 font-semibold text-[#233127]">Stock that needs attention</h2></div><button className="flex items-center gap-1 rounded-lg bg-[#157a5a] px-3 py-2 text-xs font-semibold text-white"><RiAddLine className="size-4" /> Add listing</button></div><div className="p-5"><div className="flex flex-col gap-4 rounded-lg border border-[#e1e7e2] p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-semibold">iPhone 13 OLED display</p><p className="mt-1 text-xs text-[#748077]">SKU IP13-OLED-BLK · Last updated today</p></div><span className="flex items-center gap-2"><button onClick={() => setStock(Math.max(stock - 1, 0))} className="grid size-8 place-items-center rounded-md border border-[#d5ddd6]">−</button><strong className="w-7 text-center">{stock}</strong><button onClick={() => setStock(stock + 1)} className="grid size-8 place-items-center rounded-md border border-[#d5ddd6]">+</button></span><span className="flex items-center gap-1 rounded-full bg-[#edf7ef] px-3 py-1.5 text-xs font-semibold text-[#38744f]"><RiShoppingBagLine className="size-4" /> In stock</span></div></div></article></RoleLayout>
}
