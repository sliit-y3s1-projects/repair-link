import { RiArrowRightLine, RiHeart3Line, RiMapPin2Line, RiShieldCheckFill, RiStarFill, RiTimeLine } from '@remixicon/react'
import { Link } from 'react-router'
import type { Repairer } from '../types'

type RepairerCardProps = {
  repairer: Repairer
  isSaved: boolean
  onSave: () => void
}

export function RepairerCard({ repairer, isSaved, onSave }: RepairerCardProps) {
  return <article className="group relative overflow-hidden rounded-xl border border-[#e0e4e0] bg-white p-3 transition-shadow hover:shadow-[0_8px_24px_rgba(25,49,37,.10)]"><div className="flex gap-4"><Link to={`/repairers/${repairer.id}`} className="relative block size-28 shrink-0 overflow-hidden rounded-lg sm:size-32"><img src={repairer.imageUrl} alt="" className="size-full object-cover transition-transform duration-500 group-hover:scale-105" /><span className="absolute left-2 top-2 rounded-full bg-white/95 px-2 py-1 text-[10px] font-bold text-[#27543e]">{repairer.tag}</span></Link><div className="min-w-0 flex-1 py-0.5"><div className="flex items-start justify-between gap-2"><Link to={`/repairers/${repairer.id}`} className="min-w-0"><h2 className="truncate text-[15px] font-semibold text-[#252c27]">{repairer.name}</h2><p className="mt-0.5 truncate text-sm text-[#68736b]">{repairer.specialty}</p></Link><button onClick={onSave} className="grid size-8 shrink-0 place-items-center rounded-full border border-[#e2e6e2] bg-white text-[#354038]" aria-label={`Save ${repairer.name}`}><RiHeart3Line className={`size-4 ${isSaved ? 'fill-[#157a5a] text-[#157a5a]' : ''}`} /></button></div><div className="mt-3 flex flex-wrap gap-x-3 gap-y-1 text-xs text-[#5e6a61]"><span className="flex items-center gap-1 font-semibold text-[#303833]"><RiStarFill className="size-3 text-[#d8952e]" /> {repairer.rating} <span className="font-normal text-[#778178]">({repairer.reviews})</span></span><span className="flex items-center gap-1"><RiMapPin2Line className="size-3 text-[#157a5a]" /> {repairer.distance}</span></div><div className="mt-3 flex items-center justify-between gap-2"><span className="flex items-center gap-1 text-xs text-[#387054]"><RiShieldCheckFill className="size-3.5" /> Verified repairer</span><span className="text-sm font-semibold text-[#252c27]">From {repairer.price}</span></div></div></div><Link to={`/repairers/${repairer.id}`} className="mt-3 flex items-center justify-between border-t border-[#eef0ed] px-1 pt-3 text-xs font-semibold text-[#3c4a40]"><span className="flex items-center gap-1"><RiTimeLine className="size-3.5 text-[#157a5a]" /> {repairer.availableToday ? 'Available today' : 'Next available tomorrow'}</span><span className="flex items-center gap-1 text-[#157a5a]">View profile <RiArrowRightLine className="size-3.5" /></span></Link></article>
}
