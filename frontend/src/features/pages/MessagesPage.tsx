import { useState } from 'react'
import { RiSendPlaneFill } from '@remixicon/react'
import { RoleLayout } from '@/features/dashboard/RoleLayout'

export function MessagesPage() {
  const [messages, setMessages] = useState(['Hi, can you confirm whether the screen is original?', 'Yes, I use a compatible OLED panel with a 90-day warranty.'])
  const [draft, setDraft] = useState('')
  function send() { if (draft.trim()) { setMessages((current) => [...current, draft.trim()]); setDraft('') } }
  return <RoleLayout title="Messages" description="Keep repair conversations, quotes, and job details together in one place."><section className="grid min-h-[520px] overflow-hidden rounded-xl border border-[#dfe5e0] bg-white md:grid-cols-[250px_1fr]"><aside className="border-b border-[#e6ebe7] p-4 md:border-b-0 md:border-r"><p className="rounded-lg bg-[#edf5ef] p-3 text-sm font-semibold text-[#20583d]">Kamal’s Device Care</p><p className="mt-3 rounded-lg p-3 text-sm text-[#717971] hover:bg-[#f7f9f7]">FixRight Electronics</p></aside><div className="flex min-h-[440px] flex-col p-5"><div className="border-b border-[#e6ebe7] pb-4"><p className="font-semibold">Kamal’s Device Care</p><p className="text-xs text-[#717971]">Screen repair request</p></div><div className="flex flex-1 flex-col gap-3 py-5">{messages.map((message, index) => <p key={`${message}-${index}`} className={`max-w-[80%] rounded-2xl px-4 py-3 text-sm ${index % 2 === 0 ? 'self-start bg-[#f1f4f1] text-[#344039]' : 'self-end bg-[#176b4d] text-white'}`}>{message}</p>)}</div><div className="flex gap-2 border-t border-[#e6ebe7] pt-4"><input value={draft} onChange={(event) => setDraft(event.target.value)} onKeyDown={(event) => event.key === 'Enter' && send()} className="w-full rounded-lg border border-[#d6ded8] px-3 py-3 text-sm outline-none focus:border-[#176b4d] focus:ring-2 focus:ring-[#dceee2]" placeholder="Write a message" /><button onClick={send} className="grid size-11 shrink-0 place-items-center rounded-lg bg-[#157a5a] text-white hover:bg-[#0f5d42]" aria-label="Send"><RiSendPlaneFill className="size-4" /></button></div></div></section></RoleLayout>
}
